service: min2
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: ap-northeast-1
functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: hello
          method: get